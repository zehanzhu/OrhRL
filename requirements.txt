# Main Python environment for the current Search MAS training path.
# Covers:
# - experiments/search_mas/run_train_e2e.sh
# - python -m orchrl.trainer.train
# - the external Search MAS subprocess launched during training
#
# Install a compatible local verl checkout separately, for example:
#   python -m pip install -e /path/to/verl
#
# vllm should come from the same environment as that compatible verl checkout.
# The retriever service keeps its own conda environment and is intentionally
# not included here (for example faiss-gpu and datasets).

-r mas_apps/search/requirements.txt

aiohttp>=3.9.0
codetiming>=1.4.0
hydra-core>=1.3.2
numpy>=1.26.4
omegaconf>=2.3.0
packaging>=25.0
ray>=2.53.0
regex>=2025.11.3
torch>=2.9.1
tqdm>=4.67.1
transformers>=4.57.1
wandb>=0.24.1
